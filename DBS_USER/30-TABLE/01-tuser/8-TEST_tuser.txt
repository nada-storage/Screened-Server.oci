select * from tuser;

set @var_userid = 'user1000';

set @var_username = '서에이';

set @var_username_upd = '서아이';

-- =====================================================================================================================
--
-- ins
--
-- =====================================================================================================================

-- ------------------------------------------------------------------------
CALL pc_tuser_ins(@var_userid, @var_username, @res, @res2); 
-- ------------------------------------------------------------------------
select @var_userid, @var_username;
select @res, @res2;
select id, SUBSTRING(name, 1,20), dt7, dt8 FROM tuser;
    


-- =====================================================================================================================
--
-- upd
--
-- =====================================================================================================================

-- ------------------------------------------------------------------------
CALL pc_tuser_upd(@var_userid, @var_username_upd, @res, @res2);  
-- ------------------------------------------------------------------------
select @var_userid, @var_username_upd;
select @res, @res2;
select id, SUBSTRING(name, 1,20), dt7, dt8 FROM tuser;





-- =====================================================================================================================
--
-- sel
--
-- =====================================================================================================================

-- ------------------------------------------------------------------------
CALL pc_tuser_sel(@var_userid, @username, @res, @res2);
-- ------------------------------------------------------------------------
select @var_userid, SUBSTRING(@username, 1,10);
select @res, @res2;
select id, SUBSTRING(name, 1,20), dt7, dt8 FROM tuser;




-- =====================================================================================================================
--
-- del
--
-- =====================================================================================================================

-- ------------------------------------------------------------------------
CALL pc_tuser_del(@var_userid, @res, @res2); 
-- ------------------------------------------------------------------------
select @var_userid;
select @res, @res2;
select id, SUBSTRING(name, 1,20), dt7, dt8 FROM tuser;


 


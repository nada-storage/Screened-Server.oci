select * from tlogin;

set @var_logid = 'login1000';

set @var_passwd = 'qwerty1234';
set @var_passwd_upd = 'qazwsx12!';

set @var_userid = 'user1000';



-- =====================================================================================================================
--
-- ins
--
-- =====================================================================================================================

-- ------------------------------------------------------------------------
CALL pc_tlogin_ins(@var_logid, @var_passwd, @var_userid, @res, @res2); 
-- ------------------------------------------------------------------------
select @var_logid, @var_passwd, @var_userid;
select @res, @res2;
select * from tlogin;




-- =====================================================================================================================
--
-- upd
--
-- =====================================================================================================================


-- ------------------------------------------------------------------------
CALL pc_tlogin_upd(@var_logid, @var_passwd_upd, @res, @res2);
-- ------------------------------------------------------------------------
select @var_logid, @var_passwd_upd;
select @res, @res2;
select * from tlogin;



-- =====================================================================================================================
--
-- sel
--
-- =====================================================================================================================

-- ------------------------------------------------------------------------
CALL pc_tlogin_sel(@var_logid, @passwd, @userid, @res, @res2);
-- ------------------------------------------------------------------------
select @var_logid, @passwd, @userid;
select @res, @res2;
select * from tlogin;



-- =====================================================================================================================
--
-- del
--
-- =====================================================================================================================

-- ------------------------------------------------------------------------
CALL pc_tlogin_del(@var_logid, @res, @res2); 
-- ------------------------------------------------------------------------
select @var_logid;
select @res, @res2;
select * from tlogin;


